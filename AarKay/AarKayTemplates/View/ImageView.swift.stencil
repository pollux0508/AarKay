{% extends "View.swift.stencil" %}

{% block type %}{% if base %}{{base}}{% else %}UIImageView{% endif %}{% endblock %}

{% block init %}

    public override init(image: UIImage?) {
        super.init(image: image)
        init{{name}}()
    }
{% endblock %}
